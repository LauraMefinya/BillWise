<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BillWise.ViewModels"
             x:Class="BillWise.Views.RegisterPage"
             x:DataType="vm:RegisterViewModel"
             BackgroundColor="#3498DB"
             NavigationPage.HasNavigationBar="False"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False">

    <Grid>
        <ScrollView VerticalOptions="End">
            <Grid RowDefinitions="Auto, Auto">
                
                <!-- Back Button & Header -->
                <Grid Grid.Row="0" Margin="20,40,20,30" ColumnDefinitions="Auto,*">
                    <Label Text="←" FontSize="26" FontAttributes="Bold" TextColor="White" VerticalOptions="Center">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoBackCommand}" />
                        </Label.GestureRecognizers>
                    </Label>
                    <Label Grid.Column="1" Text="Create Account" FontSize="28" FontAttributes="Bold" TextColor="White" VerticalOptions="Center" HorizontalOptions="Center" Margin="-20,0,0,0"/>
                </Grid>

                <!-- Registration Form -->
                <Border Grid.Row="1" BackgroundColor="White" StrokeThickness="0" Margin="0" Padding="30,40,30,50">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="40,40,0,0"/>
                    </Border.StrokeShape>
                    
                    <VerticalStackLayout Spacing="16">
                        
                        <!-- Email Input -->
                        <VerticalStackLayout Spacing="6">
                            <Label Text="Email Address" FontSize="13" TextColor="#6B7280"/>
                            <Border Stroke="#E5E7EB" StrokeThickness="1" StrokeShape="RoundRectangle 12" BackgroundColor="#F9FAFB" Padding="16,4" HeightRequest="60">
                                <HorizontalStackLayout Spacing="16">
                                    <Label Text="✉️" FontSize="18" VerticalOptions="Center" />
                                    <Entry Text="{Binding Email}" Placeholder="name@example.com" Keyboard="Email" WidthRequest="250" BackgroundColor="Transparent" TextColor="#111827" PlaceholderColor="#9CA3AF" VerticalTextAlignment="Center"/>
                                </HorizontalStackLayout>
                            </Border>
                        </VerticalStackLayout>

                        <!-- Password Input -->
                        <VerticalStackLayout Spacing="6">
                            <Label Text="Password" FontSize="13" TextColor="#6B7280"/>
                            <Border Stroke="#E5E7EB" StrokeThickness="1" StrokeShape="RoundRectangle 12" BackgroundColor="#F9FAFB" Padding="16,4" HeightRequest="60">
                                <HorizontalStackLayout Spacing="16">
                                    <Label Text="🔒" FontSize="18" VerticalOptions="Center" />
                                    <Entry Text="{Binding Password}" Placeholder="At least 6 characters" IsPassword="True" WidthRequest="250" BackgroundColor="Transparent" TextColor="#111827" PlaceholderColor="#9CA3AF" VerticalTextAlignment="Center"/>
                                </HorizontalStackLayout>
                            </Border>
                        </VerticalStackLayout>
                        
                        <!-- Confirm Password Input -->
                        <VerticalStackLayout Spacing="6">
                            <Label Text="Confirm Password" FontSize="13" TextColor="#6B7280"/>
                            <Border Stroke="#E5E7EB" StrokeThickness="1" StrokeShape="RoundRectangle 12" BackgroundColor="#F9FAFB" Padding="16,4" HeightRequest="60">
                                <HorizontalStackLayout Spacing="16">
                                    <Label Text="🛡️" FontSize="18" VerticalOptions="Center" />
                                    <Entry Text="{Binding ConfirmPassword}" Placeholder="Repeat your password" IsPassword="True" WidthRequest="250" BackgroundColor="Transparent" TextColor="#111827" PlaceholderColor="#9CA3AF" VerticalTextAlignment="Center"/>
                                </HorizontalStackLayout>
                            </Border>
                        </VerticalStackLayout>

                        <!-- Register Button -->
                        <Button Text="Sign Up" Command="{Binding RegisterCommand}" BackgroundColor="#3498DB" TextColor="White" CornerRadius="12" HeightRequest="56" FontAttributes="Bold" FontSize="16" Margin="0,24,0,0"/>
                        
                        <!-- Activity Indicator -->
                        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="#3498DB" HorizontalOptions="Center" Margin="0,10,0,0"/>

                    </VerticalStackLayout>
                </Border>
            </Grid>
        </ScrollView>
    </Grid>
</ContentPage>
